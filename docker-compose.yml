services:
  cibot:
    image: mrexz/cibot:latest
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    logging:
      driver: json-file
      options:
        max-size: "15m"
        max-file: "3"